[general]
# Exit immediately after copy or save -- no lingering window.
early-exit = true

# Persist clipboard contents after satty closes.
copy-command = "wl-copy --type image/png"

# Tools: pointer, crop, line, arrow, rectangle, text, marker, blur, brush
initial-tool = "brush"
