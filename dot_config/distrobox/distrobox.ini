[base-applications-toolbox]
image=quay.io/toolbx-images/archlinux-toolbox:latest
init=false
nvidia=false
pull=true
root=false
replace=true
start_now=false
additional_packages="base-devel git pass gnupg browserpass"
init_hooks="if ! command -v yay &> /dev/null; then echo '📦 Installing yay AUR helper...' && cd /tmp && git clone https://aur.archlinux.org/yay.git && cd yay && makepkg -si --noconfirm && cd .. && rm -rf yay; fi && echo '📦 Installing AUR packages...' && yay -S --noconfirm beekeeper-studio storageexplorer teams-for-linux zen-browser-bin legcord obs-studio-git bruno-bin polypane anytype-bin vivaldi-snapshot vivaldi-snapshot-ffmpeg-codecs cursor-bin || true && sudo pacman -Scc --noconfirm && echo '✅ base-applications-toolbox setup complete!'"
