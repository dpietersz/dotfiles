# Global mise config — intentionally empty.
#
# As of the brew migration (see ~/dotfiles/Plans/mise-to-brew if present),
# mise is used ONLY for per-project tools, declared in each project's own
# mise.toml (e.g. ~/dev/Projects/homelab/mise.toml).
#
# Global CLI tools live in ~/.config/brewfiles/Brewfile{,.darwin,.linux}.
# AI/CLI npm tools (pi, auggie) live in
#   .chezmoiscripts/run_onchange_after_13-install-npm-globals.sh.tmpl.
#
# Do NOT re-add tools here. If a tool genuinely should be global, add it to
# the appropriate Brewfile instead.

[tools]
