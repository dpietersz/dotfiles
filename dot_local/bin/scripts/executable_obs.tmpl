#!/bin/bash
{{- if .remote }}
echo "obs wrapper: not applicable on remote/server" >&2
exit 0
{{- else }}
set -euo pipefail

{{ if .hasNvidia -}}
exec env __NV_PRIME_RENDER_OFFLOAD=1 __GLX_VENDOR_LIBRARY_NAME=nvidia /usr/bin/obs "$@"
{{- else -}}
exec /usr/bin/obs "$@"
{{- end }}
{{- end }}
