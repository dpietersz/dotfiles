#!/bin/bash

set -euo pipefail

{{- if or .remote .isMacOS }}
# Skip on remote environments or macOS
exit 0
{{- end }}

# Install superfile (its installer script handles all the placement)
bash -c "$(curl -sLo- https://superfile.dev/install.sh)"

# mcp-hub moved to .chezmoiscripts/run_onchange_after_13-install-npm-globals.sh.tmpl
# (installed via bun alongside pi + auggie).
