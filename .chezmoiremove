# Files/directories chezmoi removes from the destination on each apply.
# Once gone, listing them here is a no-op. Safe to leave entries here
# permanently as a tombstone of removed tooling.

# voxd voice dictation — replaced by voquill (2026-05-21), then by vibe-typer.
.local/bin/scripts/voxd-ctrl.sh
.local/bin/start-voxd.sh
.config/voxd
.local/share/voxd

# voquill voice dictation — replaced by vibe-typer (2026-05-21).
.config/com.voquill.desktop
.local/share/com.voquill.desktop
.cache/com.voquill.desktop

# Old per-domain toolboxes — consolidated into single udx-toolbox (2026-05-22).
# Container images deleted from ghcr; local containers can be cleaned up with:
#   distrobox rm browser-toolbox data-toolbox notetaking-toolbox
.config/distrobox/browser-toolbox.ini
.config/distrobox/data-toolbox.ini
.config/distrobox/notetaking-toolbox.ini

# Terax removed entirely (2026-05-22). Local RPM cleanup:
#   sudo rpm-ostree uninstall terax && sudo systemctl reboot
.local/share/applications/terax.desktop
.local/state/terax
.cache/terax

# kitty-zellij-main / alacritty-zellij-main wrappers collapsed into the
# base wrappers (2026-05-25). kitty-zellij now always attaches to "main".
.local/bin/kitty-zellij-main
.local/bin/alacritty-zellij-main

# zen browser wrapper — replaced by host-side helium-browser-bin (2026-05-27).
# The wrapper exec'd into a `browser-toolbox` distrobox that no longer exists.
# Default-browser wiring now lives in run_onchange_after_18-set-default-browser.
.local/bin/zen

# toggle-workspaces — replaced by toggle-workspace-monitor (2026-05-27).
# Old script moved ALL named workspaces between monitors with a hardcoded LG
# assumption (--to-lg / --to-laptop). New script toggles the ACTIVE workspace
# between laptop (eDP-*) and any connected external monitor.
.local/bin/scripts/toggle-workspaces

# zellij session helpers — replaced by native kitty sessions (kitty-session,
# 2026-05-29). Migrated off zellij/tmux to kitty's goto_session workflow.
.local/bin/kitty-zellij-pick
.local/bin/zellij-switch

# LocalSend moved from udx-toolbox (Arch) to ubuntu-gui-toolbox (2026-07-13).
# The Arch build's Dart VM stopped initialising against glibc 2.43 — black
# window, empty stderr. The stale export points at a binary that no longer
# exists in udx-toolbox, so remove it; the init_hook re-exports the working
# one from ubuntu-gui-toolbox as ubuntu-gui-toolbox-localsend_app.desktop.
.local/share/applications/udx-toolbox-localsend.desktop
